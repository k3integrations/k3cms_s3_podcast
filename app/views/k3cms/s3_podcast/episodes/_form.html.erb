<%# TODO: this page should go away once it is fully inline editable %>

<%= form_for(episode) do |f| %>
  <% if episode.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(episode.errors.count, "error") %> prevented this episode from being saved:</h2>
    <ul>
    <% episode.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 4 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
